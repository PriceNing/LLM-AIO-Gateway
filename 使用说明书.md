# LLM AIO Gateway 使用说明书

## 目录

1. [项目简介](#1-项目简介)
2. [快速开始](#2-快速开始)
3. [管理面板](#3-管理面板)
4. [供应商管理](#4-供应商管理)
5. [用户与 API Key](#5-用户与-api-key)
6. [API 使用指南](#6-api-使用指南)
7. [高级功能](#7-高级功能)
8. [配置说明](#8-配置说明)
9. [常见问题](#9-常见问题)

---

## 1. 项目简介

LLM AIO Gateway 是一个统一的 API 网关，它将多个 LLM 供应商（OpenAI、Anthropic、MiniMax、DeepSeek 等）的接口统一为 OpenAI/Anthropic 兼容格式。您只需维护一个网关地址，即可在多个模型供应商之间自由切换，无需修改客户端代码。

### 核心特性

- **多供应商统一接口**：支持任意 OpenAI 兼容或 Anthropic 兼容的 API 供应商
- **多协议支持**：Chat Completions、Completions、Messages（Anthropic 原生）、Responses 四种 API 格式
- **流式与非流式**：所有端点均支持 SSE 流式输出
- **路由规则**：根据用户名、API Key 或模型名灵活路由到不同目标
- **图片预处理**：自动将图片送入视觉模型生成描述，然后剥离原始图片
- **管理面板**：Web 界面管理供应商、用户、路由规则等
- **SQLite 存储**：无需额外数据库，开箱即用

---

## 2. 快速开始

### 2.1 环境要求

- Python 3.10+
- pip

### 2.2 安装与启动

```bash
# 1. 克隆/下载项目
cd LLM-AIO

# 2. 安装依赖
pip install -r requirements.txt

# 3. 启动服务
python main.py
```

服务默认运行在 http://localhost:8000。

```bash
# 编辑 config.json 中的 host 和 port 字段可修改监听地址和端口
```

### 2.3 首次使用流程

1. 打开 http://localhost:8000/ 进入管理面板
2. 系统提示初始化管理员账号，创建用户名和密码
3. 登录后，先添加一个供应商（见第 4 章）
4. 创建一个普通用户（见第 5 章）
5. 为用户生成 API Key（以 sk-aio- 开头）
6. 使用该 API Key 调用网关

### 2.4 Docker 部署

```bash
# 使用 docker-compose（推荐）
docker compose up -d

# 或手动构建
docker build -t llm-aio-gateway .
docker run -d -p 8000:8000 -v ./data:/app/data llm-aio-gateway
```

Docker 部署会自动映射端口 8000，数据和配置持久化到宿主机的 ./data/ 目录（含 data.db 和 config.json）。

---

## 3. 管理面板

### 3.1 登录

浏览器打开 http://localhost:8000/。首次访问会要求创建管理员账号。

### 3.2 管理面板功能一览

管理面板左侧导航包含以下页面：

| 页面 | 功能 |
|------|------|
| 用户管理 | 创建/编辑/删除用户，为用户生成和管理 API Key |
| 提供商 | 添加/编辑/删除 LLM 供应商 |
| 模型管理 | 查看和编辑每个供应商下的模型，设置预处理器 |
| 路由规则 | 配置模型透明路由 |
| 视觉模型注入 | 配置图片预处理器（视觉模型），管理模型的预处理器开关 |
| 统计 | 查看最近请求时间线、模型使用分布、各模型请求统计 |

### 3.3 统计仪表盘

统计页面显示：
- 最近请求的时间线（每分钟成功/失败数）
- 模型使用分布（饼图）
- 各模型请求统计（总数、失败数、Token 数）

---

## 4. 供应商管理

### 4.1 添加供应商

在管理面板的 Provider 页面点击"添加 Provider"，填写：

| 字段 | 说明 | 示例 |
|------|------|------|
| ID | 供应商唯一标识 | minimax、deepseek |
| Provider 类型 | 接口协议类型 | openai 或 Anthropic |
| API 地址 | API 基础 URL | https://api.minimax.chat/v1 |
| API Key | 你的 API 密钥 | sk-xxxxx |
| 备注 | 可选说明 | |

### 4.2 Provider 类型说明

| 类型 | 适用场景 | 说明 |
|------|----------|------|
| openai | 标准的 OpenAI 兼容接口 | 大多数供应商使用此类型 |
| Anthropic | Anthropic 原生 Messages API | DeepSeek Anthropic 端点等 |
| openai（api.openai.com/azure.com） | OpenAI 官方或 Azure | liteLLM 特殊处理 |

### 4.3 模型列表

添加供应商后，点击"刷新模型"自动从供应商拉取可用模型列表。也可以手动添加模型。

每个模型可以设置：
- **启用/禁用**：控制模型是否可用
- **预处理器**：开关，控制是否对该模型启用图片预处理（见第 7.2 节）

### 4.4 编辑供应商

点击编辑按钮可以修改供应商的全部信息，包括模型列表的启用状态和预处理器设置。

---

## 5. 用户与 API Key

### 5.1 创建用户

在管理面板的用户页面创建用户。用户信息包括用户名和备注。

### 5.2 生成 API Key

为用户生成 API Key，格式为 sk-aio- 开头的随机字符串。

创建 API Key 时可以设置：
- **密钥名称**（便于管理）
- **允许的模型列表**：限制该 Key 可以调用的模型，留空表示不限制

### 5.3 API Key 鉴权

调用 API 时在 HTTP Header 中传入：

```
Authorization: Bearer sk-aio-xxxxxxxxxx
```

支持的 Header 格式：
- Authorization: Bearer <key>

---

## 6. API 使用指南

所有 API 端点同时支持 /v1/... 前缀和根级别路径。

### 6.1 Chat Completions — /v1/chat/completions

OpenAI 标准的对话补全接口。

```bash
curl http://localhost:8000/v1/chat/completions \
  -H "Authorization: Bearer sk-aio-xxxxx" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4o",
    "messages": [{"role": "user", "content": "你好"}],
    "temperature": 0.7,
    "max_tokens": 1024
  }'
```

**参数说明：**

| 参数 | 类型 | 说明 |
|------|------|------|
| model | string | 模型名称（必填） |
| messages | array | 消息列表（必填） |
| temperature | float | 温度参数 (0-2)，默认 0.7 |
| max_tokens | integer | 最大输出 Token 数 |
| max_completion_tokens | integer | 同 max_tokens，二选一 |
| stream | boolean | 是否流式输出，默认 false |
| tools | array | 工具定义（Function Calling） |
| tool_choice | string/object | 工具选择策略 |

### 6.2 Completions — /v1/completions

OpenAI 标准的文本补全接口（非对话）。

```bash
curl http://localhost:8000/v1/completions \
  -H "Authorization: Bearer sk-aio-xxxxx" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4o",
    "prompt": "写一首关于春天的诗",
    "max_tokens": 200
  }'
```

### 6.3 Messages — /v1/messages

Anthropic 原生 Messages API 格式。当目标供应商为 Anthropic 类型时使用直通模式，不走 liteLLM 转换。

```bash
curl http://localhost:8000/v1/messages \
  -H "Authorization: Bearer sk-aio-xxxxx" \
  -H "anthropic-version: 2023-06-01" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "claude-3-opus",
    "max_tokens": 1024,
    "messages": [{"role": "user", "content": "你好"}]
  }'
```

**参数说明：**

| 参数 | 类型 | 说明 |
|------|------|------|
| model | string | 模型名称（必填） |
| messages | array | Anthropic 格式消息（必填） |
| max_tokens | integer | 最大输出 Token（必填） |
| system | string/array | 系统提示词 |
| stream | boolean | 是否流式 |
| tools | array | 工具定义 |
| tool_choice | string/object | 工具选择策略 |

**Anthropic 内容块格式：**

```json
{"type": "text", "text": "你好"}
{"type": "image", "source": {"type": "base64", "media_type": "image/jpeg", "data": "..."}}
{"type": "tool_use", "id": "tu_xxx", "name": "get_weather", "input": {...}}
{"type": "tool_result", "tool_use_id": "tu_xxx", "content": "晴朗"}
```

### 6.4 Responses — /responses

OpenAI Responses API 格式（Codex 兼容）。

```bash
curl http://localhost:8000/responses \
  -H "Authorization: Bearer sk-aio-xxxxx" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4o",
    "input": "你好"
  }'
```

支持 input 为字符串或消息数组。支持 tools、instructions（相当于系统提示）等参数。

### 6.5 模型列表 — /v1/models

获取所有可用模型：

```bash
curl http://localhost:8000/v1/models \
  -H "Authorization: Bearer sk-aio-xxxxx"
```

### 6.6 流式调用

所有端点设置 "stream": true 即可启用 SSE 流式输出。

Chat Completions 流式响应格式：

```
data: {"id":"chatcmpl-xxx","object":"chat.completion.chunk","choices":[{"delta":{"content":"你好"},"index":0}]}
data: {"id":"chatcmpl-xxx","object":"chat.completion.chunk","choices":[{"delta":{"content":"，"},"index":0}]}
data: [DONE]
```

Messages 流式响应（Event Stream 格式）：

```
event: content_block_start
data: {"type":"text","index":0}

event: content_block_delta
data: {"type":"text_delta","index":0,"delta":"你好"}

event: message_stop
data: {}
```

### 6.7 多模态（图片）输入

Chat Completions 格式的图片输入：

```json
{
  "model": "gpt-4o",
  "messages": [
    {
      "role": "user",
      "content": [
        {"type": "text", "text": "这张图里有什么？"},
        {"type": "image_url", "image_url": {"url": "data:image/jpeg;base64,/9j/..."}}
      ]
    }
  ]
}
```

Messages 格式的图片输入：

```json
{
  "type": "image",
  "source": {
    "type": "base64",
    "media_type": "image/jpeg",
    "data": "/9j/..."
  }
}
```

Responses API 格式的图片输入：

```json
{
  "type": "input_image",
  "image_url": "data:image/jpeg;base64,/9j/..."
}
```

### 6.8 工具调用（Function Calling）

```bash
curl http://localhost:8000/v1/chat/completions \
  -H "Authorization: Bearer sk-aio-xxxxx" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4o",
    "messages": [{"role": "user", "content": "北京天气怎么样？"}],
    "tools": [{
      "type": "function",
      "function": {
        "name": "get_weather",
        "description": "获取城市天气",
        "parameters": {
          "type": "object",
          "properties": {
            "city": {"type": "string"}
          },
          "required": ["city"]
        }
      }
    }],
    "tool_choice": "auto"
  }'
```

---

## 7. 高级功能

### 7.1 路由规则

路由规则允许将请求透明地重定向到不同的模型或供应商。规则配置在管理面板的"路由规则"页面。

**规则字段：**

| 字段 | 说明 |
|------|------|
| 名称 | 规则标识 |
| 启用 | 是否启用该规则 |
| 用户名 | 匹配特定用户（留空匹配所有） |
| API Key 模式 | API Key 子串匹配（留空匹配所有） |
| 匹配模型 | 支持 * 通配符，如 MiniMax-M2* |
| 目标模型 | 重定向到的模型名称 |
| 目标供应商 | 重定向到的供应商 ID |

**匹配逻辑：**
1. 按规则顺序匹配，第一条匹配的规则生效
2. 用户名精确匹配或留空
3. API Key 子串匹配或留空
4. 模型名支持 * 通配符匹配

**示例：** 将对 gpt-4o 的请求路由到 MiniMax：

| 字段 | 值 |
|------|------|
| 匹配模型 | gpt-4o |
| 目标模型 | example-model |
| 目标供应商 | minimax |

### 7.2 图片预处理（视觉模型注入）

此功能将图片自动发送到视觉模型生成描述，替换原始图片为描述文本。适用于不支持图片输入的纯文本模型。

**工作流程：**
1. 在 config.json 的 `preprocessors` 对象中配置一个或多个预处理器，每个指定一个视觉模型供应商，设置 `"enabled": true`
2. 在管理面板中，将目标模型的"预处理器"开关打开
3. 用户发送包含图片的请求到该模型时，网关自动：a) 将图片送入 config.json 中第一个启用的视觉模型获取描述 b) 从消息中剥离原始图片 c) 将描述文本注入到消息中

**预处理器配置（config.json）：**

```json
{
  "preprocessors": {
    "my-vision": {
      "api_base": "http://127.0.0.1:8080/v1",
      "model": "gpt-4o",
      "api_key": "sk-xxx",
      "timeout": 60,
      "max_images": 5,
      "max_tokens": 1024,
      "prompt": "Please describe this image in detail.",
      "enabled": true
    }
  }
}
```

**行为细节：**
- 只有 **当前轮次**（同一次对话的最新用户消息）的图片会被描述
- 历史消息中的图片只被剥离，不会重新描述
- 同一轮次中重复的图片（如用户消息和 tool_result 中都有的图片）会被去重
- 描述结果会缓存（默认最多 500 条，FIFO 淘汰），避免重复调用视觉模型
- **注意：** 即使预处理器 `enabled` 设为 `false`，网关仍会从消息中剥离所有图片（替换为 `[image: removed]`），只是不会生成描述。这意味着设为该预处理器后，无论开关状态，原始图片数据都不会到达后端模型

### 7.3 工具调用断路器

当模型连续返回工具调用但无文本输出超过一定次数时，断路器自动移除 tools 和 tool_choice 参数，防止模型陷入工具调用循环。

- 默认阈值：20 次（可在 config.json 的 tool_only_limit 调整）
- 计数器在模型生成文本内容或出错时重置
- 按对话隔离，不同对话互不影响

### 7.4 DeepSeek 推理缓存

DeepSeek 的推理模式要求在多轮对话中回传 reasoning_content。网关自动：

1. 在发送请求前，从缓存中取出上一轮的推理内容，注入到 assistant 消息中
2. 在收到响应后，将本次的推理内容存入缓存
3. 按对话隔离，确保多轮对话的推理连贯性

### 7.5 Think 标签处理

DeepSeek 推理模式在文本内容中使用 <think>...</think> 标签包裹推理过程。网关自动：
- 在流式响应中缓存 <think> 块内的内容，</think> 后才输出给客户端
- 在非流式响应中剥离 <think> 标签
- 正确处理嵌套 <think> 标签

---

## 8. 配置说明

### 8.1 config.json

服务器启动时从 config.json 读取。可通过 LLM_GATEWAY_CONFIG 环境变量指定路径。

```json
{
  "host": "0.0.0.0",
  "port": 8000,
  "database": "data.db",
  "logging": {
    "enabled": true,
    "level": "INFO",
    "log_dir": "logs",
    "retention_days": 30,
    "console": false
  },
  "defaults": {
    "max_tokens": 16384,
    "temperature": 0.7,
    "tool_only_limit": 20,
    "min_image_max_tokens": 2000,
    "session_ttl_hours": 12,
    "request_log_max": 200,
    "reasoning_cache_ttl": 1800,
    "reasoning_cache_max_size": 1000,
    "tool_only_turns_ttl": 600,
    "tool_only_turns_max_size": 2000,
    "image_cache_max_size": 500
  },
  "preprocessors": {}
}
```

**defaults 配置项说明：**

| 键 | 默认值 | 说明 |
|------|--------|------|
| max_tokens | 16384 | 客户端未传时的默认最大 Token 数 |
| temperature | 0.7 | 默认温度参数 |
| tool_only_limit | 20 | 工具调用断路器阈值 |
| session_ttl_hours | 12 | 管理面板会话过期时间 |
| request_log_max | 200 | 最近请求日志保留条数 |
| reasoning_cache_ttl | 1800 | DeepSeek 推理缓存过期时间（秒） |
| reasoning_cache_max_size | 1000 | 推理缓存最大条目数 |
| min_image_max_tokens | 2000 | 包含图片的请求 max_tokens 下限 |
| tool_only_turns_ttl | 600 | 工具调用计数器 TTL（秒） |
| tool_only_turns_max_size | 2000 | 工具调用计数器最大条目数 |
| image_cache_max_size | 500 | 图片描述缓存最大条目数 |

### 8.2 环境变量

| 变量 | 说明 |
|------|------|
| LLM_GATEWAY_CONFIG | 指定 config.json 路径 |

---

## 9. 常见问题

### 9.1 如何查看日志？

日志默认输出到控制台和 logs/ 目录。每个日期一个子目录，每个级别一个文件。

```bash
tail -f logs/2026-05-22/access.log
tail -f logs/2026-05-22/app.log
```

### 9.2 模型格式是 provider/model 还是 model ？

两个格式都支持。当使用 provider/model 复合格式时（如 minimax/example-model），网关直接定位到指定供应商的指定模型。使用简单模型名时，按 provider 顺序返回第一个匹配该模型名的已启用供应商。

### 9.3 如何启用图片预处理？

1. 在 config.json 的 preprocessors 对象中添加预处理器配置并设置 `"enabled": true`
2. 在管理面板中，找到目标模型，将"预处理器"开关打开
3. 保存后生效

### 9.4 为什么有些供应商返回 400 错误？

常见原因：
- 某些供应商（如 MiniMax）不支持 OpenAI 的 tool_choice: "auto" 格式，网关会自动转换为 null
- 某些供应商不接受 stream_options 参数，网关自动移除
- 某些供应商要求严格的消息交替（user/assistant/tool），网关自动合并连续同角色消息
- 某些供应商不接受 additionalProperties 或 strict 字段，网关自动清理

### 9.5 Docker 运行后 data.db 在哪里？

数据文件默认为运行目录的 data.db。Docker 部署时已映射卷，文件保存在宿主机的 ./data/data.db。

### 9.6 如何重启服务器？

- 直接运行：按 Ctrl+C 停止，重新运行 python main.py
- Windows 下端口被占用：taskkill //F //IM python.exe
- Docker：docker compose restart

### 9.7 路由规则不生效？

检查：
1. 规则是否已启用
2. 规则顺序是否正确（越具体的规则放在越前面）
3. 通配符 * 是否使用正确
4. 目标模型和供应商是否存在且启用

### 9.8 如何更新供应商模型列表？

在管理面板的 Provider 页面，点击对应供应商的"刷新模型"按钮。新发现的模型会追加到列表，**不会删除**已有的模型。

---

## 附录 A：端点速查

| 路径 | 方法 | 说明 | 鉴权 |
|------|------|------|------|
| /auth/status | GET | 检查是否已有管理员 | 无 |
| /auth/setup | POST | 初始化管理员 | 无 |
| /auth/login | POST | 管理员登录 | 无 |
| /auth/logout | POST | 管理员登出 | Session |
| /auth/me | GET | 获取当前管理员信息 | Session |
| /auth/password | PUT | 修改管理员密码 | Session |
| /admin/providers | GET/POST | 供应商列表/创建 | Session |
| /admin/providers/{id} | PUT/DELETE | 编辑/删除供应商 | Session |
| /admin/providers/{id}/refresh | POST | 刷新单个供应商模型列表 | Session |
| /admin/providers/refresh-all | POST | 刷新所有供应商模型列表 | Session |
| /admin/models | GET | 查看所有可用模型 | Session |
| /admin/users | GET/POST | 用户列表/创建 | Session |
| /admin/users/{username} | PUT/DELETE | 编辑/删除用户 | Session |
| /admin/users/{username}/api-keys | POST | 为用户创建 API Key | Session |
| /admin/users/{username}/api-keys/{key} | PUT/DELETE | 编辑/删除 API Key | Session |
| /admin/routing-rules | GET/POST | 路由规则列表/创建 | Session |
| /admin/routing-rules/{id} | PUT/DELETE | 编辑/删除路由规则 | Session |
| /admin/stats | GET | 查看统计数据 | Session |
| /admin/stats/reset | POST | 清空统计数据 | Session |
| /admin/preprocessors | GET | 预处理器配置列表 | Session |
| /admin/preprocessors/{id} | PUT/DELETE | 编辑/删除预处理器 | Session |
| /admin/preprocessors/fetch-models | GET | 从视觉模型服务器拉取模型列表 | Session |
| /admin/models/preprocessor | PUT | 切换模型的预处理器开关 | Session |

| /v1/chat/completions | POST | Chat Completions | API Key |
| /v1/completions | POST | Completions | API Key |
| /v1/messages | POST | Messages (Anthropic) | API Key |
| /v1/models | GET | 模型列表 | API Key |
| /responses | POST | Responses API | API Key |

> 注：所有代理端点（/v1/chat/completions 等）同时也注册在根级别（/chat/completions 等）。

---

## 附录 B：支持的工具链

- **FastAPI** — Web 框架
- **liteLLM** — 多供应商 LLM 调用库
- **SQLite** — 数据存储
- **Pydantic** — 数据验证
- **FastAPI StaticFiles** — 静态文件服务
- **Chart.js** — 仪表盘图表

---

*文档生成时间：2026-05-22*
